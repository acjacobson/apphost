services:
  lab:
    build:
      context: ./repo
      dockerfile: deploy/Dockerfile
    container_name: lab
    restart: unless-stopped
    environment:
      APP_NAME: lab
      APP_PORT: 8080
    expose:
      - "8080"
    networks:
      - web

networks:
  web:
    external: true
